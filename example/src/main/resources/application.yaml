spring:
  application:
    name: example
  datasource:
    primary:
      jdbc-url: jdbc:postgresql://localhost:5432/sezdb
      username: andruf
      password: andruf
      driver-class-name: org.postgresql.Driver
    replica:
      jdbc-url: jdbc:postgresql://localhost:5433/sezdb
      username: andruf
      password: andruf
      driver-class-name: org.postgresql.Driver
  docker:
    compose:
      file: /home/andruf/Work/IdeaProjects/example/example/docker-compose.yaml
  jpa:
    hibernate:
      ddl-auto: update
  flyway:
    clean-disabled: false
app:
  repository.type: jpa
